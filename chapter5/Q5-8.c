#include <stdio.h>

int main(int argc, char const *argv[])
{
	int xnary;
	int numberOfdigits;
	int sum = 0;
	int sum_backup;
	int counter = 0;

	scanf("%d", &xnary);
	scanf("%d", &numberOfdigits);

	for (i = 0; i < numberOfdigits)
	{
		sum *= xnary;
		scanf("%d", &digit);
		sum += digit;
	}
	sum_backup = sum;

	scanf("%d", &xnary);
	while (sum > 0)
	{
		sum /= xnsary;
		if (sum > 0)
			counter++;	
	}
	printf("%d\n", );

	return 0;
}
// 2017-8-30